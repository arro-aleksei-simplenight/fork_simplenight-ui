import React from 'react';
import { render } from '@testing-library/react';
import SortByButton from './SortByButton';

describe('Paragraph', () => {
  test('Renders the paragraph component', () => {
    render(<SortByButton isOpen onClick={() => {}} />);
  });
});
